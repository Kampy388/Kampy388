# Kopiere diese Datei zu .env und trage die echten Passwörter ein.
# .env wird NIEMALS committet (siehe .gitignore).
#
# Für die meisten Anbieter solltest du ein App-Passwort statt deines normalen
# Kontopassworts verwenden (GMX, Web.de, GMail mit 2FA etc. bieten das an).

GMX_PASSWORD=
FIRMA_PASSWORD=
